---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

<h1 class="bump">{{ content.headline }}</h1>
<p class="bump">{{ content.elevator }}</p>

{% set maxPosts = collections.posts.length | min(3) %}
<h2>Featured work</h2>

{% set postslist = collections.posts | head(-3) %}
{% set postslistCounter = collections.posts | length %}
{% include "postslist.njk" %}

<p>More work can be viewed in <a href="{{ '/posts/' | url }}">the archive</a>.</p>
